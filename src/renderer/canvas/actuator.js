import { BodyFlags } from "../../physics/body"
import { arc } from "./renderer"

export function renderActuator(ctx, { body }) {
  // ctx.strokeStyle = "rgba(0, 255, 0, 0.8)"
  // ctx.lineWidth = 0.004
  // ctx.beginPath()
  // ctx.arc(body.pos.x, body.pos.y, body.radius * 1.3, ...arc(0))
  // ctx.stroke()
}
