import { BodyFlags } from "../../physics/body"
import { arc } from "./renderer"

export function renderFoodSeeker(ctx, { body }) {
  // ctx.strokeStyle = "rgba(255, 255, 0, 0.8)"
  // ctx.lineWidth = 0.004
  // ctx.beginPath()
  // ctx.arc(body.pos.x, body.pos.y, body.radius * 1.3, ...arc(2))
  // ctx.stroke()
}
